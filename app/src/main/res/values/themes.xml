<resources xmlns:tools="http://schemas.android.com/tools">
  <!-- Base application theme. -->
  <style name="Theme.Petda" parent="Theme.MaterialComponents.DayNight.NoActionBar">
    <!-- Primary brand color. -->
    <item name="colorPrimary">@color/main</item>
    <item name="colorControlActivated">@color/main</item>
    <item name="colorControlNormal">@color/underLine</item>
    <item name="android:textSelectHandle">@color/main</item>
    <item name="android:textColorHighlight">@color/main</item>
    <item name="android:textCursorDrawable">@color/main</item>
    <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
    <!-- Customize your theme here. -->
  </style>
</resources>